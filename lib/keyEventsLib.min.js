function key_down(e){var o=e.keyCode||e.which;keyboard.key[o]=!0}function key_up(e){var o=e.keyCode||e.which;keyboard.key[o]=!1}console.log("KeyboardLib loaded"),window.addEventListener("keydown",key_down,!1),window.addEventListener("keyup",key_up,!1);class Keyboard{constructor(){this.key=[]}getKey(e){return this.key[e]}}keyboard=new Keyboard;

/*
console.log("KeyboardLib loaded")

window.addEventListener("keydown", key_down, false);
window.addEventListener("keyup", key_up, false);

function key_down(e) {
    var key = e.keyCode || e.which;
    keyboard.key[key] = true;
}    
    
function key_up(e) {
    var key = e.keyCode || e.which;
    keyboard.key[key] = false;
}

class Keyboard {
    constructor() {
        this.key = [];
    }
    
    getKey(e) {
        return this.key[e];
    }
}

keyboard = new Keyboard();
*/

/*
Author: @Favo https://twitter.com/Favo_Gaming
Created at: 13.02.2020 09:27 
*/